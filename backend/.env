$env:SMTP_HOST="smtp.gmail.com"
$env:SMTP_PORT="587"
$env:SMTP_USER="Event Assistant"
$env:SMTP_PASS="dinhngocAN123@@"
$env:FROM_EMAIL="andinh1443@gmail.com"
REMINDER_INTERVAL=60

# chạy app (hoặc pytest/test):
# python -m uvicorn main:app --reload